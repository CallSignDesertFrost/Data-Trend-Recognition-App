data_schema.sql `sql CREATE TABLE users ( id SERIAL PRIMARY, name VARCHAR(25) NOT NULL email VARCHAR(25) UNIQUE NOT NULL);

CREATE TABLE data_points ( id SERIAL PRARY KEY, user_id INTEGER NOT NULL REFERENCES users(id), value DOUBLE PRECISION NOT NULL, timestamp TIMESTAMP NOT NULL );